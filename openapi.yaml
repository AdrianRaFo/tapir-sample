# This file has been autogenerated, don't try to edit it manually

openapi: 3.0.3
info:
  title: Sample
  version: latest
paths:
  /hello/{name}:
    get:
      tags:
      - hello-v1
      description: Return hello message
      operationId: hello
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Hello message for name
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HelloResponse'
              example:
                helloMessage: Hello Foo
        '400':
          description: Invalid received request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BadRequestResponse'
components:
  schemas:
    BadRequestResponse:
      required:
      - name
      - error
      type: object
      properties:
        name:
          type: string
          description: The received name
        error:
          type: string
          description: The failure reason
      description: Bad request response for hello call
    HelloResponse:
      required:
      - helloMessage
      type: object
      properties:
        helloMessage:
          type: string
          description: The hello message
      description: Response with a successful hello message
